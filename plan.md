# agent-probe · 四个月实施计划

> 为 Coding Agent 提供独立于应用日志的系统审计、可解释的行为关联与受限执行控制：回答“任务动了哪些文件、连了哪些网络、消耗了多少 token、是否违反策略，以及这些结论有多可靠”。

- **周期假设**：单人全职 16 周，约 80 个工作日；70 天计划工作 + 10 天集成与风险缓冲。要求具备 Linux/C/Python 基础，并复用成熟探针；不含从零学习这些技术的时间。
- **技术栈**：Linux / QEMU ARM64 / libbpf + CO-RE / uprobe + tracepoint + fentry/fexit / BPF LSM / Python 分析层。
- **被测对象**：同级目录的 code-review-agent、pyfixloop-v2，优先验证实际使用的执行路径。
- **核心贡献**：可靠采集、带证据与不确定性的行为关联、Docker 任务映射、受限文件保护，以及可复现的对照与消融实验。
- **最终交付**：可复现环境、采集器、事件账本、关联和审计引擎、执行控制、CLI/静态报告、基准任务集与实验报告。

## 1. 信任模型与范围

- agent 为无特权进程；采集器与日志由独立受保护身份持有。宿主 root、内核、采集器管理员可信。不承诺抵抗 root/内核攻击或绝对防篡改。
- 固定并验证一套 ARM64 Linux 内核、Python/OpenSSL、Docker 版本，记录配置和镜像摘要；具备 BTF 不代表所有挂点可用。
- 支持两个指定 agent 的 HTTP/1.1 JSON、SSE、chunked、gzip 路径；HTTP/2、HTTP/3、br、静态链接 TLS 和未知 API 格式显式标记为不支持。
- 任务级关联为主要承诺。调用级关联区分纯外部观测和轻量标记辅助两种模式；同进程并发等无法唯一归因的场景输出 ambiguous/unknown。
- 执行控制限定为已验证的受保护目录策略。网络和成本规则只告警，不承诺全协议网络阻断或精确账单限额。
- TLS 原文可能包含凭据与提示词，默认仅持久化必要元数据；调试原文显式启用、脱敏并设保留期限，API key 不进入日志。

## 2. 里程碑与工期

| 状态 | 里程碑 | 周期 | 人日 | 阶段出口 |
|:---:|---|---|---:|---|
| [ ] | M0 环境与能力验证 | 第 1 周 | 5 | 真实 TLS 调用点、内核观测挂点及 LSM 可用性验证 |
| [ ] | M1 请求重建与计账 | 第 2–3 周 | 10 | JSON/SSE 可解析、重放、对账，缺失数据显式标记 |
| [ ] | M2 系统事件与可靠账本 | 第 4–6 周 | 15 | 文件/进程/网络事件可核验，丢失可检测，支持离线重放 |
| [ ] | M3 任务、容器与调用关联 | 第 7–9 周 | 15 | Docker 映射、两种关联模式、并发歧义评估 |
| [ ] | M4 审计规则与报告 | 第 10 周 | 5 | CLI 可解释违规证据、成本与数据质量 |
| [ ] | M5 受限执行控制 | 第 11–12 周 | 10 | 受保护目录策略及正常操作回归通过 |
| [ ] | M6 对照实验与交付 | 第 13–14 周 | 10 | 对照、消融、性能实验及复现材料完成 |
| [ ] | 集成与风险缓冲 | 第 15–16 周 | 10 | 修复剩余问题，从干净 VM 复现并完成演示 |
| | **合计** | **16 周** | **80** | |

按表顺序推进，每阶段保留可运行版本；测试夹具从 M0 开始积累。M3 依赖 M1/M2，M5 依赖 M0 的 LSM 能力验证与 M2/M4 的事件和策略基础。缓冲期不安排新功能。

| 月份 | 可展示成果 |
|---|---|
| 第 1 月（第 1–4 周） | 两个 agent 的 LLM 请求计账与基础系统事件闭环 |
| 第 2 月（第 5–8 周） | 可靠事件账本、任务隔离与 Docker 映射原型 |
| 第 3 月（第 9–12 周） | 并发关联评估、审计报告、受限文件保护 |
| 第 4 月（第 13–16 周） | 完整实验、性能分析、复现脚本与演示交付 |

## 3. 各阶段实施内容

### M0 · 环境与能力验证（5 天）

**交付物**

- scripts/setup-vm.sh：固定 QEMU ARM64 + Ubuntu 环境，在 VM 内编译，初期不增加交叉编译负担。
- docs/00-env.md：实际内核配置、权限、工具链、依赖版本与复现步骤。
- 能力检测：BTF、tracepoint、fentry/fexit、BPF LSM；第一周就验证后续执行控制所需挂点。
- 两个 agent 各运行一次真实请求，定位实际 TLS 库，验证 SSL_read/write 及 _ex 接口；读取数据在返回后按实际字节数采集。
- 本地 TLS 测试服务、系统操作夹具、无探针性能基线。

**验收**：从空白 VM 可重复搭建；有脱敏请求/响应、挂点与版本清单。不可用挂点在本周调整环境或采用经过验证的采集替代方案；普通 fentry 不能替代 LSM 阻断。

### M1 · LLM 请求重建与计账（10 天）

**交付物**

- 先支持已有客户端的普通 JSON，再实现 SSE、chunked、跨 read 半包、gzip、连接复用与断连处理。
- 按进程实例、TLS 连接生命周期和请求序号维护状态；区分逻辑调用与物理请求，处理重试，防止重复计账。
- 提取 model、输入/输出 token、提供方返回的缓存/推理明细；确认明细是否已包含在总量中。
- usage 缺失标记 unknown，不能填零；明确区分捕获失败、解析失败和提供方未返回 usage。
- 使用版本化价格表估算费用，记录币种、计价单位与日期；不宣称等于实际账单。
- 对账脚本与离线协议回放夹具；参考 pyfixloop-v2/src/pyfixloop_v2/llm/usage.py，但不把 SDK 日志当作唯一真值。
- 协议不支持、截断和状态丢失必须可诊断；限制连接缓存、单请求内容和解压大小。

**验收**：至少 200 个受控请求，覆盖 JSON/SSE、重试、断流、usage 缺失、压缩和连接复用；捕获率、解析成功率与 usage 一致率分别报告。

### M2 · 系统事件与可靠账本（15 天）

**交付物**

- 进程：fork/exec/exit、启动标识、父子关系；处理 PID 复用、短命子进程和采集启动前已有进程。
- 文件：打开、实际读取/写入、截断、重命名、删除；结合入口与返回结果区分尝试、成功打开和实际读写。
- 建立 fd、文件身份和路径关系；覆盖相对路径、工作目录、fd 复用、临时文件替换、符号链接和容器路径视图。
- 对 write/pwrite/writev 等实际路径逐项验证；mmap 写和 io_uring 首版列为明确采集盲区，不承诺覆盖所有修改。
- 网络：TCP 连接目的与结果、UDP 发送目的；经典 UDP/TCP DNS 解析是可延期增强，端口 53 不代表全部 DNS。
- 统一事件头：schema_version、run_id、单调时钟、进程启动标识、PID/TID、cgroup、namespace、事件类型、结果与关联标识。
- 有界队列、序号与丢失计数、持久化错误告警；JSONL 事件存储和 SQLite 索引，支持恢复、查询和离线重放。
- 分开统计探针覆盖遗漏与传输/存储丢失；不能用 ring buffer 无丢失推导全量行为覆盖。

**验收**：至少 30 类操作夹具，以预期操作、返回值、文件快照和测试服务接收记录建立真值；strace/auditd 用于交叉验证。执行 30 分钟额定负载与过载测试，证明丢失可检测、重放结果一致。第 4 周冻结额定负载与资源预算。

### M3 · 可解释关联与容器归因（15 天）

**交付物**

- probe run 分配 run_id 并建立任务 cgroup，跟踪进程、请求与系统事件的完整生命周期。
- Docker 映射：任务标签 → 容器 ID → 宿主 PID/cgroup → 挂载视图；不假设 daemon 创建的容器进程属于 agent 子进程树。
- 首版限定同 VM 内可查询的 Docker；容器启动、停止和短命任务的映射竞争需要测试。
- 证据图：任务 → LLM 请求/响应 → 工具执行 → 系统事件；关联边保存依据、方法版本和置信等级。
- 外部模式：结合连接、进程血缘、时间范围生成候选；多候选时保留歧义，不把最近响应直接当作确定因果。
- 辅助模式：为两个 agent 增加最小适配器，传播 run_id/call_id/tool_id，覆盖异步上下文与子进程；应用标记属于声明，须与系统事件交叉核验。
- 可序列化关联结果、离线重算和解释接口；为时间窗、血缘、容器映射与辅助标记提供独立开关，支持消融。

**验收**：至少 200 条带独立预期关系的样本，覆盖串行、2/5 路并发、子进程和容器；分别评估同进程与多进程并发。辅助标记不能同时作为唯一真值；报告精确率、召回率、确定归因覆盖率与歧义率。

### M4 · 审计规则与报告（5 天）

**交付物**

- YAML 规则：禁止目录修改、敏感文件实际读取、目的地址白名单、估算成本超限。tests/ 是否只读由任务策略决定。
- 中断分类：协议终止标识缺失、提供方明确返回上下文错误、进程异常退出；按协议解释 finish_reason，不能把所有非 stop 结果视为断流。
- probe report：按 agent/任务/模型汇总 token、估算费用、违规证据、采集缺口与归因置信度。
- probe explain <event-id> 与静态 HTML 时间线，支持从告警回溯原始事件。
- 区分事实、推断和证据不足；未知用量与歧义关联不能在汇总时消失。

**验收**：每条规则都有正常、违规、证据不足的固定用例；原始事件可重算报告，CLI 与静态报告结论一致。

### M5 · 受限执行控制（10 天）

**交付物**

- 通过已验证的 BPF LSM hooks 实现受保护目录策略，普通 fentry 仅负责观测。
- audit/enforce 两种模式，策略在 agent 启动前安装；拒绝原因通过适配器反馈给 agent。
- 明确写入、创建、截断、删除、重命名、链接操作所需 hooks 与支持矩阵；核验符号/硬链接、继承 fd、跨目录替换、容器挂载与路径解析。
- 限制 agent 权限与挂载能力；对 mmap/io_uring 等未验证路径明确限制，不能把有限策略宣传成完整沙箱。
- 内核拒绝与日志投递解耦；明确 pinned link 生命周期、策略检查、启动失败行为和显式清理流程。
- 采集器退出不能静默解除已安装保护；日志不可用必须告警。记录审计完整性与阻断有效性的不同保证。
- 网络和成本策略维持告警，不扩展全协议阻断。

**验收**：至少 30 个受控违规/绕过用例及 30 个正常用例；支持矩阵中的违规全部阻止、正常全部通过。以系统调用返回和文件内容验证，不能仅看告警。演示“拒绝 → 原因反馈 → agent 合规重试”。

### M6 · 对照实验与交付（10 天）

**交付物**

- 20 个真实任务，两个 agent 各至少 10 个，每任务至少重复 3 次；固定输入、环境、策略与模型配置，记录费用和所有失败。
- 独立异常集：子进程行为、容器执行、并发交错、临时文件替换、重试、断流、过载、越界操作；真实任务与注入异常分别报告。
- 对照组：现有应用日志、纯外部观测、外部观测 + 辅助标记。对齐任务/文件/请求等语义事件，不能直接比较 syscall 数与高层日志条数。
- 真值来自受控操作预期、结果验证、测试服务记录和必要人工复核；分别测应用日志与 probe 的遗漏，不预设“应用层漏记 30%+”。
- 消融：时间窗基线 → 加进程血缘 → 加容器映射 → 加辅助标记；报告每项的收益、代价与失败条件。
- 性能组：无探针、仅系统探针、加入 TLS、完整审计、启用控制；记录 CPU 时间、峰值内存、耗时分布和 p50/p95。
- 分开测本地固定负载与远端 LLM 工作负载，避免网络等待稀释探针开销；置信区间按任务/运行计算，注明样本限制。
- docs/evaluation.md、脱敏数据样本、基准清单、复现脚本、演示流程、已知限制与第三方许可证说明。

**验收**：干净 VM 可搭建环境、运行两个 agent、演示文件保护并生成报告；夹具可离线复现，真实模型任务注明凭据与费用前提。未达目标的指标保留原始结果与原因，不择优选样。

## 4. 指标口径与目标

以下是待验证目标，不是已取得的结果。按协议、并发模式、任务类型和负载分组，保留分子、分母与失败样本。

| 指标 | 口径与目标 |
|---|---|
| 请求捕获率 | 支持协议中捕获的物理请求 / 测试服务接收请求 ≥98%；重试单列 |
| usage 一致率 | 已捕获且参考侧提供 usage 的请求中，字段完全一致比例 ≥98%；另报 usage 可用率 |
| 费用估算误差 | 相同请求集合与价格表下，总费用相对误差 <2%；零费用报绝对误差，缺失 usage 单列 |
| 事件完整性 | 固定额定负载下，受支持语义事件召回率 ≥99%；过载丢失可检测，覆盖盲区另列 |
| 任务级关联 | 独立真值中 precision/recall 均 ≥98%；未知归因不能从召回分母中删除 |
| 调用级关联 | 辅助模式 precision ≥95%、确定归因覆盖率 ≥90%；纯外部模式报告实测，不承诺任意并发可唯一归因 |
| 越界审计 | 支持场景中 recall ≥95%、FPR ≤5%；各规则分别报告 TP/FP/FN/TN |
| 文件保护 | 固定支持矩阵中违规全部阻止，正常回归全部通过；不外推为任意攻击下 100% 安全 |
| 中断检测 | 断流、明确上下文错误、异常退出分别测 precision/recall，目标均 ≥90% |
| 应用日志漏记率 | 对统一真值分别测应用日志和 probe 的漏记率，不预设差距 |
| 性能 | 探索目标：固定本地基准的任务耗时增幅 ≤10%；CPU/内存单列，不能直接沿用论文指标 |

精确率为正确关联/告警占所有给出关联/告警的比例；召回率以全部真值为分母；确定归因覆盖率为有唯一归因的事件占待归因事件的比例。高精确率必须与覆盖率一同解释。

## 5. 风险、决策点与缓冲

| 风险 | 决策点与处理 |
|---|---|
| ARM64 挂点或 LSM 不兼容 | 第 1 周验证，优先调整固定 VM；阻断不可行须记录范围变更，不能用告警替代并宣布完成 |
| 协议重建超时 | 第 3 周先保证真实 JSON 路径；SSE/gzip 可使用缓冲完成，HTTP/2 不进入范围 |
| 并发归因不达标 | 第 9 周核验误关联与覆盖率；保留歧义，辅助模式提高证据强度，不篡改评估口径 |
| 文件保护覆盖不足 | 第 12 周冻结支持矩阵；必要时缩小保护场景并记录偏差，未覆盖操作明确限制 |
| API 费用或服务波动 | 第 2 周按实际价格和样本规模制定调用预算；开发使用本地夹具/回放，真实任务另行验证 |
| 工期超支 | 优先延期 DNS 增强、HTML 美化和额外兼容版本；保留可靠采集、关联评估、核心文件保护与实验 |

第 15–16 周共 10 天：用于延期项、集成缺陷、环境重建与最终文档。若关键项仍未完成，应交付实际完成清单和剩余工作估算，不以缩小统计分母满足指标。

## 6. 明确不做

- 通用 Agent trace 平台、分布式服务、多租户系统和重型 Dashboard。
- 任意框架、任意容器运行时、所有 TLS/HTTP 实现的兼容。
- 完整策略 DSL、完整沙箱、抵抗 root 的防篡改系统。
- 全部文件修改路径、全协议网络阻断、精确账单级限额。
- macOS/Windows 原生 eBPF 采集；macOS 仅作为 Linux VM 宿主。

## 7. 参考与复用原则

| 来源 | 用途 |
|---|---|
| [AgentSight 论文](https://arxiv.org/abs/2508.02736) | TLS 与内核事件关联路线；其性能结果不能直接转写为本项目 CPU 指标 |
| [Linux BPF LSM 文档](https://docs.kernel.org/bpf/prog_lsm.html) | 执行控制挂点与拒绝语义 |
| [OpenSSL SSL_read 文档](https://docs.openssl.org/3.0/man3/SSL_read/) | SSL_read/SSL_read_ex 接口与返回语义 |
| eunomia-bpf/agentsight | 探针实现参考；复用前核对许可证、固定 commit，区分复用部分和新增贡献 |

其他工具只按实际需求引入。新增组件须说明解决的具体问题、维护成本与可验证收益。
